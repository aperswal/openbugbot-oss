#!/bin/sh

required_node_major=24
current_node_major="$(node -p 'process.versions.node.split(".")[0]' 2>/dev/null || true)"

if [ "$current_node_major" = "$required_node_major" ]; then
  pnpm check
else
  npx --yes --package=node@24 -c 'pnpm check'
fi
